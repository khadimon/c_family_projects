<!--

Program Author: Khadichabonu Valieva

Assignment: Little League Database

Description: TeamPage.xaml of the app
    
-->
    
    <?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="littleLeageDatabase.teamPage"
             Title="teamPage">
    <StackLayout>
        <Label Text="Team Name"/>
        <Entry Text="{Binding Team.Name}"/>
        <Label Text="Team Mascot"/>
        <Entry Text="{Binding Team.Mascot}"/>
        <Button Text="Save" Command="{Binding SaveTeamCommand}"/>
    </StackLayout>
</ContentPage>